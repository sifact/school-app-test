import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#3377FF",
            yellow: "#FBD062",
            green: "#7AB259",
        },
    },
    typography: {
        fontFamily: "Quicksand",
    },
});
